Add the slide deck here as `slides.pdf` (preferred) or `slides.pptx`.

TODO: this deck is not yet added. It should cover, in order:

1. Problem — who, what, why it hurts (see `../docs/problem-statement.md`)
2. Solution — what we built and how it works (see `../docs/solution-overview.md`)
3. Demo / architecture — key technical highlights (see `../docs/architecture.md`)
4. IBM technology integration — where and how Bob/watsonx is used (currently: not yet — see
   the "Known Limitations" section of `../README.md` for the planned integration point)
5. Impact — what this could become beyond the hackathon
